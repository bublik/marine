= simple_form_for(@personal, remote: true, html: {format: :js, multipart: true, role: 'form',
class: 'registration '}) do |f|
  = token_tag
  = f.error_notification
  .row
    .col-md-8
      = f.input :available_from, as: :string, autocomplete: 'off', placeholder: 'yyyy-mm-dd', input_html:{class: 'datepicker'}
      = f.association :rank
      = f.association :salary, label_method: :amount
      = f.input :surname
      = f.input :name
      = f.input :middle_name
      = f.input :pp_dob, as: :string, autocomplete: 'off', placeholder: 'yyyy-mm-dd', input_html:{class: 'datepicker'}
      = f.input :pp_pob
      = f.association :country
      = f.association :citizenship
      = f.association :marital
      = f.input :sex, collection: [:m, :f], as: :radio_buttons
      = f.association :eye
      = f.association :hair, label_method: :color
      = f.association :height, label_method: :height
      = f.association :weight, label_method: :weight
      = f.association :overallsize, label_method: :overall_size
      = f.association :shoe, label_method: :shoes_size
      = f.input :taxation_id_code
      = f.input :note
      %hr/

      = image_tag(@personal.photo_url) if @personal.photo?
      = f.input :photo, as: :file
      //= f.hidden_field :photo_cache

  .text-right
    = f.button :submit, 'Next', class: 'btn btn-primary btn-lg btn-next'
